describe("firstStrike is a coin toss to dtermine which party goes first", function() {
  it will chose a random number between 1 and 3 which assigns either penguins or commies, function() {
    expect(coin == 1).toBe(penguins);
  }
})
